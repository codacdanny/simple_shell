#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>
#include <sys/types.h>
#include <limits.h>

char **populate(char *s);
char *_strdup(char *s);
char *trim(char *s);

#endif
